import { UnavailableScreen } from "@/components/page-state";

export default function AdminPage() {
  return <UnavailableScreen title="운영 콘솔은 아직 제공되지 않아요" description="RBAC와 감사 API가 준비되면 권한 있는 운영자에게만 열립니다." />;
}
