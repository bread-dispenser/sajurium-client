import { UnavailableScreen } from "@/components/page-state";

export default function CalendarPage() {
  return <UnavailableScreen title="시기 캘린더는 아직 제공되지 않아요" description="날짜별 실제 계산 API와 검증 기준이 준비되면 제공됩니다." />;
}
