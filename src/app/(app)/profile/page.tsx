import { UnavailableScreen } from "@/components/page-state";

export default function ProfilePage() {
  return <UnavailableScreen title="내 프로필 편집은 준비 중입니다" description="현재 프로필 생성·목록·삭제는 사람 보관함에서 실제 서버로 처리합니다. 수정 API 전환은 스냅샷 보존 검증 후 열립니다." />;
}
